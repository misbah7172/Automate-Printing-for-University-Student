services:
  - type: web
    name: autoprint-backend
    runtime: node
    plan: free
    region: oregon
    buildCommand: cd backend && npm ci --only=production
    startCommand: cd backend && node minimal-start.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000